Cryptography-Project
====================

Project that uses the Hill Cipher method to encode and decode text. Accepts all ASCII input, and uses matrices along with inverse "keys" to perform encoding and decoding. Writes encoded message to a text file to be decoded.
